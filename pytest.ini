[pytest]
; Coleta apenas a suíte oficial em tests/.
; Evita coletar a cópia clonada _github_connectagro/ (causava
; ImportPathMismatchError por duplicar tests.conftest).
testpaths = tests
norecursedirs = _github_connectagro .venv backups data docs migrations scripts
